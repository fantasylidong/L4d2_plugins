#if defined boost_inc_
    #endinput
#endif
#define boost_inc_

// I really think lower SrouceMod shouldnt be supported any longer.
// This code force the version of SourceMod higher than 1.7
// But u can delete it, of course :)
#pragma newdecls required

#include <boost/client>
#include <boost/entity>
#include <boost/infected>
#include <boost/player>